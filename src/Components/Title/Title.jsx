import React from "react";
import "./Title.css";

const Title = ({ Title }) => {
  return (
    <div className="title">
      <p></p>
      <h2>{Title}</h2>
    </div>
  );
};

export default Title;
